% Dimensions
C_width = sqrt(M);      %width of the cart
C_height = C_width;     %heigth of the cart

S_radius = m^(1/3);     %radius of the sphere


wy = C_height/2;

rectangle('Position',[x-W/2,y-H/2,W,H],'Curvature',.1,'FaceColor',[1 0.1 0.1])
